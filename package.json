{
  "name": "acy-first-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ jrcarlos2000/acy-first-graph",
    "create-local": "graph create --node http://localhost:8020/ jrcarlos2000/acy-first-graph",
    "remove-local": "graph remove --node http://localhost:8020/ jrcarlos2000/acy-first-graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 jrcarlos2000/acy-first-graph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
